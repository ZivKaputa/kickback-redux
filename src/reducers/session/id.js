// import { SESSION_UPDATE } from '../../actions/session/updateSession.js'

const DEFAULT_STATE = 'test'

export default function id(state = DEFAULT_STATE, action) {
  switch (action.type) {
    default:
      return state
  }
}